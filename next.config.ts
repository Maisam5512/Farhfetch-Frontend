import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['localhost' , 'dependable-cow-a08d589b62.strapiapp.com' , 'dependable-cow-a08d589b62.media.strapiapp.com'], // or your API host like 'your-api.com'
  },
};

export default nextConfig;
